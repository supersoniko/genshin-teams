import {makeStyles, Theme} from '@material-ui/core/styles'

/**
 * Template-scoped styles which should be used as a hook
 * inside {{ pascalCase name }} functional template
 */

{{#if hasStyleProps}}
export interface StyleProps {
	placeholderProps?: boolean
}
{{/if}}

const useStyles = ({{#if hasStyleProps}}{placeholderProps}:StyleProps{{/if}}) =>
	makeStyles((theme: Theme) => ({
		root: {
			background: theme.palette.warning.main,
			{{#if hasStyleProps}}
			color: (): string => {
				let color = 'red'
				if (placeholderProps) {
					color = 'green'
				}

				return color
			}
			{{/if}}
		}
	}))()

export default useStyles
